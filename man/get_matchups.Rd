% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matchups.R
\name{get_matchups}
\alias{get_matchups}
\title{Extract matchups realizing a contrast between two profiles}
\usage{
get_matchups(
  tasks,
  A,
  B,
  outcome,
  profiles = c("1", "2"),
  sep = "_",
  winner = "A_wins",
  keep = NULL,
  check_contrast = TRUE
)
}
\arguments{
\item{tasks}{A data frame in task-wide form (see \code{\link[=as_tasks]{as_tasks()}}).}

\item{A, B}{Named lists mapping attribute stems to pinned levels for each side.}

\item{outcome}{Name (stem) of the per-profile choice-indicator columns
(e.g. \code{"chosen"} for columns \code{chosen_1}, \code{chosen_2}). Values must be 0/1.}

\item{profiles}{Length-2 character vector of the profile suffixes. Defaults to
\code{c("1", "2")}.}

\item{sep}{Separator between an attribute stem and its profile suffix.}

\item{winner}{Name for the returned binary outcome column. Defaults to
\code{"A_wins"}.}

\item{keep}{Character vector of columns to carry through to the result. If
\code{NULL} (default), every column that is \emph{not} profile-specific (i.e. does not
end in \code{sep} + a profile suffix) is kept: task keys, respondent covariates,
weights, and so on.}

\item{check_contrast}{If \code{TRUE}, error on a contrast that is not mutually
exclusive.}
}
\value{
A tibble of the matched tasks: the \code{keep} columns plus the \code{winner}
column (0/1).
}
\description{
A \emph{matchup} is the subset of choice tasks in which one profile carries the
attributes pinned in \code{A} and the other carries the attributes pinned in \code{B}
(in either display position). \code{get_matchups()} filters \code{tasks} to those rows
and returns them with a single binary outcome, \code{winner}, equal to 1 when the
A-side profile was chosen. This turns an arbitrary conjoint into a clean
pairwise sub-experiment ready for estimation (see \code{\link[=afcp]{afcp()}}).
}
\details{
\code{A} and \code{B} are named lists mapping attribute names (the stems, without the
profile suffix) to the level each side is pinned to. An empty list leaves that
side unconstrained.

For the contrast to be well defined, A and B must be \strong{mutually exclusive}:
they must pin at least one shared attribute to different levels. Otherwise a
single profile can satisfy both sides and the "estimate" reflects display
position (primacy), not a real difference. When \code{check_contrast = TRUE} (the
default) an invalid contrast raises an error explaining why; see
\code{\link[=valid_contrast]{valid_contrast()}}.
}
\examples{
tasks <- data.frame(
  study_id = "s1", resp_id = c(1, 1, 2, 2),
  party_1  = c("R", "D", "R", "R"), party_2 = c("D", "R", "R", "D"),
  chosen_1 = c(1, 0, 1, 1),          chosen_2 = c(0, 1, 0, 0)
)
get_matchups(tasks, A = list(party = "R"), B = list(party = "D"),
             outcome = "chosen")
}
