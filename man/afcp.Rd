% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afcp.R
\name{afcp}
\alias{afcp}
\title{Average Feature Choice Probability from a matchup}
\usage{
afcp(
  matchups,
  outcome = "A_wins",
  by = NULL,
  clusters = NULL,
  weights = NULL,
  se_type = NULL,
  min_clusters = 2L
)
}
\arguments{
\item{matchups}{A data frame from \code{\link[=get_matchups]{get_matchups()}}.}

\item{outcome}{Name of the binary outcome column. Defaults to \code{"A_wins"}.}

\item{by}{Optional character vector of grouping columns (e.g. \code{"study_id"});
one AFCP is returned per group.}

\item{clusters}{Optional name of the cluster column (e.g. \code{"resp_id"}) for
cluster-robust standard errors.}

\item{weights}{Optional name of a survey-weight column.}

\item{se_type}{Standard-error type passed to \code{estimatr::lm_robust()}. The
default \code{NULL} lets \code{lm_robust()} choose its own default: \code{"CR2"} when
\code{clusters} is supplied, \code{"HC2"} otherwise. Set explicitly (e.g. \code{"stata"})
only to override that.}

\item{min_clusters}{When \code{clusters} is supplied, groups with fewer than this
many distinct clusters are dropped rather than estimated (a single-cluster
AFCP has no usable sampling variance). Ignored without \code{clusters}.}
}
\value{
A tidy tibble, one row per group (or a single row when \code{by} is
\code{NULL}): the grouping columns (if any) plus \code{estimate} (the AFCP),
\code{std.error}, \code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}, \code{df}, and
\code{n} (observations). This is the tidy-tibble form, not a fit object, so that
many per-group AFCPs compose directly into a meta-analysis; call
\code{estimatr::lm_robust()} yourself if you need the fit.
}
\description{
A convenience wrapper that closes the loop on the matchup workflow: given the
output of \code{\link[=get_matchups]{get_matchups()}}, it estimates the probability that the A-side
profile is chosen (the AFCP), optionally within respondent-clustered,
weighted groups. The estimation itself is a one-liner over
\code{estimatr::lm_robust()}; this function exists only to make
\code{as_profiles() |> as_tasks() |> get_matchups() |> afcp()} a complete
workflow. For richer estimands (AMCE, marginal means) use 'cregg' or 'cjoint'.
}
\examples{
\donttest{
tasks <- data.frame(
  study_id = "s1", resp_id = rep(1:20, each = 2),
  party_1  = rep(c("R", "D"), 20), party_2 = rep(c("D", "R"), 20),
  chosen_1 = rep(c(1, 0), 20),      chosen_2 = rep(c(0, 1), 20)
)
m <- get_matchups(tasks, list(party = "R"), list(party = "D"),
                  outcome = "chosen")
if (requireNamespace("estimatr", quietly = TRUE)) {
  afcp(m, by = "study_id", clusters = "resp_id")
}
}
}
