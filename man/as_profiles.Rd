% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{as_profiles}
\alias{as_profiles}
\title{Reshape task-level (wide) conjoint data to profile-level (long)}
\usage{
as_profiles(
  tasks,
  task_keys,
  profiles = c("1", "2"),
  sep = "_",
  profile_to = "profile"
)
}
\arguments{
\item{tasks}{A data frame in task-wide form.}

\item{task_keys}{Character vector of column names identifying a task. Together
with \code{profile_to} these key the output.}

\item{profiles}{Character vector of the profile values to unstack. Defaults to
\code{c("1", "2")}.}

\item{sep}{Separator between an attribute name and the profile suffix.}

\item{profile_to}{Name for the new profile-index column in the output.}
}
\value{
A tibble in profile-long form.
}
\description{
The inverse of \code{\link[=as_tasks]{as_tasks()}}. Columns matching \verb{x\{sep\}\{profile value\}} are
pivoted back into one row per profile per task; columns that do not match
(task-level or respondent-level covariates) are recycled across the profiles.
}
\examples{
tasks <- data.frame(
  task_id  = c(1, 2),
  party_1  = c("R", "D"), party_2 = c("D", "R"),
  chosen_1 = c(1, 0),      chosen_2 = c(0, 1)
)
as_profiles(tasks, task_keys = "task_id")
}
\seealso{
\code{\link[=as_tasks]{as_tasks()}} for the inverse.
}
