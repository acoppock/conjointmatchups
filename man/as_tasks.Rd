% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape.R
\name{as_tasks}
\alias{as_tasks}
\title{Reshape profile-level (long) conjoint data to task-level (wide)}
\usage{
as_tasks(profiles, task_keys, profile, outcome = NULL, sep = "_")
}
\arguments{
\item{profiles}{A data frame in profile-long form: one row per candidate
profile per task.}

\item{task_keys}{Character vector of column names that jointly identify a
choice task (e.g. \code{c("study_id", "resp_id", "task_id")}). These are carried
through unchanged.}

\item{profile}{Name of the column indexing the profile within a task (values
such as \code{1}/\code{2}). Its values become the column-name suffixes.}

\item{outcome}{Optional name of the choice-indicator column. If supplied, the
resulting per-profile outcome columns are coerced to numeric (they are
character after pivoting, like every attribute).}

\item{sep}{Separator between an attribute name and the profile suffix.
Defaults to \code{"_"}.}
}
\value{
A tibble with one row per task: \code{task_keys} plus, for every other
input column \code{x} and every profile value \code{p}, a column \verb{x\{sep\}p}.
}
\description{
Each choice task appears as two profile rows in the input. \code{as_tasks()}
pivots those rows into a single wide row per task, suffixing every
non-key column with the profile index (e.g. \code{party} becomes \code{party_1} and
\code{party_2}). This is the representation used for modeling the binary choice
and for \code{\link[=get_matchups]{get_matchups()}}.
}
\examples{
profiles <- data.frame(
  task_id = c(1, 1, 2, 2),
  profile = c(1, 2, 1, 2),
  party   = c("R", "D", "D", "R"),
  chosen  = c(1, 0, 0, 1)
)
as_tasks(profiles, task_keys = "task_id", profile = "profile",
         outcome = "chosen")
}
\seealso{
\code{\link[=as_profiles]{as_profiles()}} for the inverse.
}
