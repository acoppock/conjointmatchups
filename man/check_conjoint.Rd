% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{check_conjoint}
\alias{check_conjoint}
\title{Check that task-wide data is a valid forced-choice conjoint}
\usage{
check_conjoint(tasks, outcome = "chosen", profiles = c("1", "2"), sep = "_")
}
\arguments{
\item{tasks}{A data frame in task-wide form (see \code{\link[=as_tasks]{as_tasks()}}).}

\item{outcome}{Name (stem) of the per-profile choice-indicator columns.}

\item{profiles}{Length-2 character vector of profile suffixes.}

\item{sep}{Separator between an attribute stem and its profile suffix.}
}
\value{
Invisibly, \code{tasks}. Called for its side effect of erroring on a
violated invariant.
}
\description{
Verifies the invariants a binary forced-choice conjoint must satisfy in
task-wide form: the two per-profile outcome columns exist, their values are
0/1, and exactly one profile is chosen in every task. Generalizes the
structural checks used when cleaning a conjoint study.
}
\examples{
tasks <- data.frame(chosen_1 = c(1, 0), chosen_2 = c(0, 1))
check_conjoint(tasks)
}
